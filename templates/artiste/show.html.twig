{% extends 'base.html.twig' %}

{% block title %}Artiste{% endblock %}

{% block body %}
    <h1>Artiste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artiste.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ artiste.nom }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td><img src={{ artiste.photo }} alt='{{ artiste.nom }}'></td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('artiste_index') }}">Retour à la liste</a>

    {% if app.user %}
    <a class="btn btn-info" href="{{ path('artiste_edit', {'id': artiste.id}) }}">Modifier</a>

    {{ include('artiste/_delete_form.html.twig') }}
    {% endif %}

{% endblock %}
