{% extends 'base.html.twig' %}

{% block title %}Artiste{% endblock %}

{% block body %}
    <div class="p-5">
        <h1>{{ artiste.nom }}</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ artiste.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ artiste.nom }}</td>
                </tr>
                <tr>
                    <th>Photo</th>
                    <td><img class="rounded-circle" height="150" src="{{ artiste.photo }}" alt='{{ artiste.nom }}'></td>
                </tr>
            </tbody>
        </table>


        {% if app.user %}
        <a class="btn btn-info" href="{{ path('artiste_index') }}">Retour à la liste</a>
        <a class="btn btn-info mt-2 mb-2" href="{{ path('artiste_edit', {'id': artiste.id}) }}">Modifier</a>

        {{ include('artiste/_delete_form.html.twig') }}
        {% endif %}

    </div>

{% endblock %}
